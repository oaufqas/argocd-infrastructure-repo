apiVersion: v1
kind: Service
metadata:
  name: vault-service
  namespace: vault
spec:
  publishNotReadyAddresses: true
  type: NodePort
  selector:
    app.kubernetes.io/name: vault
    component: server
  ports:
  - name: vault
    port: 8200
    targetPort: 8200
    nodePort: 31976